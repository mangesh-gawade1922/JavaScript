//=========Predefined Functions==============
/** eval: Evaluate a string and returs a value
 *  parseInt: Parses a string argument and returns an integer of the specified radix or base
 *  parseFloat: Parses a string argument and returns a floating point number
 *  escape: Returns the hexadecimal encoding of an argument
 *  unescape: Returns an ASCII string for the specified value
 */

let x = 3;
let y = 7;
let z = 9;

console.log(eval(x+y));
console.log(eval(z));
